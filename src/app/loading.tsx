import React from 'react';

const LoadingMessage = () => {
    return (
        <div className=' text-4xl font-bold flex justify-center items-center container max-auto'>
            Loading text
        </div>
    );
};

export default LoadingMessage;